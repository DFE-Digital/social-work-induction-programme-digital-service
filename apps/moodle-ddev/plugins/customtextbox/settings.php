<?php
defined('MOODLE_INTERNAL') || die();

function assignsubmission_customtextbox_save_instance_settings($data) {
    global $DB;
    // We'll let the submission plugin class handle its own settings.
}